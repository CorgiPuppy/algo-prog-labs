#include <stdio.h>
#include <stdlib.h>

int main() {
	int year = 15;
	float add = -97.6f;
	const char quot = '*';
	printf("year = %d\tadd = %f\tquot = %c\nyear = %d\nadd = %f\nquot = %c", year, add, quot, year, add, quot);
}